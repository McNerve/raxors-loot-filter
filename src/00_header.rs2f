/*@ define:module:header
hidden: true
name: header
*/

/*
  Raxor's Loot Filter
  Later modules win (apply overwrites). rule is terminal.

  display → loot_order → hide → locations → categories → value → rares → alerts → final
*/

meta {
    name = "Raxor's Loot Filter";
    description = "OSRS ground loot. Categories, then value, then rares. Alerts last.";
}
